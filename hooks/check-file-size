#!/bin/bash
# PreToolUse guard: block full-file reads on large files.
# The cross-platform implementation lives in hooks/shunt_guard.py.
DIR="$(cd -P "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if command -v python3 >/dev/null 2>&1; then
  exec python3 "$DIR/shunt_guard.py" --kind read "$@"
fi
# Fail-open if python3 is unavailable (shunt-local requires python3).
exit 0
