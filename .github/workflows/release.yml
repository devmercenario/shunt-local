name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  id-token: write

jobs:
  release:
    name: Build, sign and publish
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Build source archive and checksums
        run: |
          git archive --format=tar.gz -o shunt-local.tar.gz HEAD
          sha256sum shunt-local.tar.gz > SHA256SUMS

      - uses: sigstore/cosign-installer@v3

      - name: Generate SBOM
        uses: anchore/sbom-action@v0
        with:
          path: .
          format: spdx-json
          output-file: shunt-local.spdx.json
          artifact-name: shunt-local-sbom

      - name: Sign checksums (keyless)
        run: |
          cosign sign-blob --yes \
            --output-signature SHA256SUMS.sig \
            --output-certificate SHA256SUMS.pem \
            SHA256SUMS

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v3
        with:
          files: |
            shunt-local.tar.gz
            SHA256SUMS
            SHA256SUMS.sig
            SHA256SUMS.pem
            shunt-local.spdx.json
          generate_release_notes: true
